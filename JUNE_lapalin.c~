#include<stdio.h>
#include<string.h>
int main(){

  int t,i;
  char ar[1001];
  int freq[26],freq2[26];
  scanf("%d",&t);
  while(t--){
    scanf("%s",&ar);

    for(i=0;i<26;i++){
      freq[i] = 0;
    }
    int len = strlen(ar);
    int half_len = len/2;
    for(i=0;i<half_len;i++){
      
    }

  }

}
